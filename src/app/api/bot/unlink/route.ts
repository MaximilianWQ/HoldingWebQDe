import { NextResponse } from "next/server";

/**
 * POST /api/bot/unlink — ЗАКРЫТ 22.09.2026, вслед за кабинетом.
 *
 * ПОЧЕМУ. Отвязка у людей убрана решением владельца: при ней
 * гигабайты, купленные на сайте, оставались в ботовской сущности и
 * пропадали (`docs/bot/TZ_BYPASS_MERGE.md`). В кабинете кнопку убрали
 * и маршрут `/api/user/telegram-unlink` закрыли — и в том же
 * комментарии написали, что закрыть дверь, оставив окно, нельзя.
 *
 * ЭТОТ МАРШРУТ И БЫЛ ТЕМ ОКНОМ. Его зовёт кнопка отвязки В БОТЕ
 * (`app/handlers/user/site_link.py:344` → `services/site_link/
 * service.py:132` → `client.py:215`), то есть ровно тот путь, которым
 * человек из жалобы и потерял 800 ГБ. Пока он открыт, решение
 * владельца выполнено только наполовину.
 *
 * ПОЧЕМУ 410, А НЕ 404. Бот считает `404 NOT_LINKED` УСПЕХОМ («сайт
 * про эту связку не знает — значит мы уже там, где хотели быть») и
 * вычищает свои строки. Ответить 404 значило бы порвать связку
 * односторонне: у бота её нет, у нас есть. `410` в их ветке
 * `status == 200 or site_forgot_us` не проходит — человек увидит
 * «не получилось», а обе стороны останутся связанными.
 *
 * ЧТО ПРИ ЭТОМ РАБОТАЕТ. Связка — целиком: дни и гигабайты обеих
 * сторон складываются. Отвязка живёт у админа
 * (`/api/admin/users/manage`, `action: "unlink-telegram"`), где видно
 * обе сущности и можно разобраться с гигабайтами руками.
 *
 * Снимается вместе с решением развилки из ТЗ, не раньше.
 */
export async function POST() {
  return NextResponse.json(
    {
      success: false,
      code: "UNLINK_DISABLED",
      error: "Unlink is disabled: bypass GB would be lost. Admin-only for now.",
    },
    { status: 410 }
  );
}
