import { NextResponse } from "next/server";

/**
 * POST /api/user/telegram-unlink — ЗАКРЫТ (владелец, 22.09.2026).
 *
 * Здесь была отвязка из кабинета с выбором стороны: подписка остаётся
 * на сайте или возвращается боту. Маршрут закрыт вместе с кнопкой —
 * убрать кнопку и оставить маршрут значило бы закрыть дверь, оставив
 * окно: отвязку можно было бы выполнить запросом напрямую.
 *
 * ПОЧЕМУ ЗАКРЫТО. Разбор жалобы (`docs/bot/TZ_BYPASS_MERGE.md`)
 * показал: при отвязке гигабайты, купленные НА САЙТЕ, оставались в
 * ботовской сущности и для человека пропадали. Починка упирается в
 * развилку, где платны оба пути:
 *
 *   · перенести байты — у человека меняется ключ обхода, приложение
 *     надо настраивать заново;
 *   · передать сущность — бот навсегда теряет её из виду, потому что
 *     ищет сущность обхода ПО ИМЕНИ и только по нему; дальше его
 *     покупки либо не долетают, либо создают вторую сущность.
 *
 * Пока развилка не решена, отвязка людям не показывается. Сама логика
 * (`unlinkTelegramAccount`) цела и не удалена: ею пользуется админка,
 * где человек в контуре и видит последствия, и она же понадобится,
 * когда развилку решат.
 *
 * СВЯЗКА НЕ ЗАТРОНУТА. Она работает и складывает верно: дни и
 * гигабайты обеих сторон суммируются, повторная связка ничего не
 * удваивает (`decideMerge`, защита от зеркала прошлой суммы).
 *
 * Возвращается 410 Gone, а не 404: маршрут существовал и был убран
 * намеренно — по коду это видно, и в журнале это отличимо от опечатки
 * в адресе.
 */
export async function POST() {
  return NextResponse.json(
    {
      success: false,
      error: "Отвязка Telegram временно недоступна. Напишите в поддержку — отвяжем вручную.",
    },
    { status: 410 }
  );
}
